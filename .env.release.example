# Copy to .env.release (gitignored). Used by `pnpm release:desktop` for notarization.
# A "Developer ID Application" identity must also be in the login keychain.
APPLE_ID=you@example.com
APPLE_APP_SPECIFIC_PASSWORD=xxxx-xxxx-xxxx-xxxx
APPLE_TEAM_ID=TEAMID1234
