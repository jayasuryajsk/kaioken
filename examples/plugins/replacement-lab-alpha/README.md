# Replacement Lab

Install this plugin together with `replacement-lab-beta` to exercise Kaioken's
exclusive replacement behavior. Both register a thread list and a Markdown
file opener.

- Automatic mode selects Alpha first because plugin IDs are deterministic.
- Settings can pin Kaioken, Alpha, or Beta.
- **Embed Kaioken original** exercises the instance-bound `Original` renderer.
- **Crash** exercises the owner crash fallback.
- Disabling Alpha while Automatic is selected reveals Beta.

The fixtures are intentionally diagnostic rather than useful thread lists or
file viewers.
