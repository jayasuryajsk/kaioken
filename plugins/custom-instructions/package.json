{
  "name": "kaioken-plugin-custom-instructions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Add persistent custom instructions to agent tasks on this kaioken host.",
  "engines": {
    "bb": ">=0.0"
  },
  "bb": {
    "name": "Custom instructions",
    "description": "Add persistent custom instructions to agent tasks on this kaioken host.",
    "branding": {
      "icon": "EditFile"
    },
    "server": "./server.ts"
  },
  "keywords": [
    "kaioken-plugin"
  ],
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@get-kaioken/plugin-sdk": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "npm:@typescript/typescript6@^6.0.2",
    "typescript-7": "npm:typescript@^7.0.2",
    "vitest": "^4.1.1"
  }
}
